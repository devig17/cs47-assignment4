/*import { Pressable, StyleSheet, SafeAreaView, Text, View, Dimensions, Image } from "react-native";
import { Images, Themes } from "./assets/Themes";
const windowWidth = Dimensions.get('window').width;
const windowHeight = Dimensions.get('window').height;

const SpotifyAuth = ({getSpotifyAuth}) => {
    <Pressable onPress={getSpotifyAuth}>
      <View style={styles.welcomeButton}>
        <Image 
        style={styles.welcomeImg}
        source={Images.spotify}
        />
        <Text style={styles.welcomeText}> CONNECT WITH SPOTIFY </Text>
      </View>
    </Pressable>
}

export default SpotifyAuth;

const styles = StyleSheet.create({
  welcomeButton: {
    width: windowWidth * 0.65,
    height: windowHeight * 0.05,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: "center",
    backgroundColor: Themes.colors.spotify,
    borderRadius: 99999
  },
  welcomeText: {
    color: Themes.colors.white,
    alignItems: 'center',
  },
  welcomeImg: {
    width: windowHeight * 0.05 * 0.70,
    height: windowHeight * 0.05 * 0.70,
    justifyContent: 'center',
    alignItems: 'center'
  }
});
*/